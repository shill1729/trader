# # Pick stock
# ticker <- "NTDOY"
# dat <- time_series(ticker, period = "daily")
#
# # Compute both arithmetic and log returns
# returns <- daily_returns(dat$adj_close)
# x <- returns$log
# print(plot(x))
# # GBM profile
# m <- gbmProfile(x)
# print(m)
